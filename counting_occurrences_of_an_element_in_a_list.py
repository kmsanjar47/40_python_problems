# Counting occurrences of an element in a list using the count() method.

colors = ['red', 'blue', 'red', 'green', 'blue', 'blue']
count_blue = colors.count('blue')

print('Occurrences of Blue:', count_blue)
