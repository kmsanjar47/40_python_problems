# Finding maximum and minimum values in a list using max() and min() functions.

numbers = [23, 56, 2, 34, 65]

max_number = max(numbers)
min_number = min(numbers)

print('Maximum:', max_number)
print('Minimum:', min_number)
